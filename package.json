{
  "name": "defi-wallet-hardhat",
  "version": "1.0.0",
  "description": "DeFi Wallet - per-user vault with ETH/ERC20 deposit & withdraw; Hardhat project",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy:local": "npx hardhat run --network localhost scripts/deploy.js",
    "deploy:goerli": "npx hardhat run --network goerli scripts/deploy.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "dotenv": "^16.0.0",
    "hardhat": "^2.17.0"
  }
}
